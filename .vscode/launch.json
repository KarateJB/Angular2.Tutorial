{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch index.html",
            "program": "${workspaceRoot}/src/app/app.component.html",
            "cwd": "${workspaceRoot}"
        }
    ]
}
